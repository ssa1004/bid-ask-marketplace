version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: market
      POSTGRES_USER: market
      POSTGRES_PASSWORD: market
    ports: ["5432:5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U market"]
      interval: 5s

  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s

  kafka:
    image: apache/kafka:3.8.0
    ports: ["9092:9092"]
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      # listener 를 두 개로 분리 — 호스트에서 ./gradlew bootRun 으로 띄운 앱(EXTERNAL)과
      # compose 네트워크 안의 컨테이너 서비스(INTERNAL)가 둘 다 붙을 수 있게.
      #   EXTERNAL : 호스트 → localhost:9092
      #   INTERNAL : 컨테이너 끼리 → kafka:29092 (broker 간 통신도 INTERNAL)
      # 단일 listener(advertised: localhost:9092)면 컨테이너가 localhost 를 자기 자신으로
      # 풀어 broker 를 못 찾고, 단일 listener(advertised: kafka:9092)면 호스트가 kafka
      # 호스트명을 못 풀어 producer 가 hang/실패한다 — 두 listener 가 양쪽을 모두 만족.
      KAFKA_LISTENERS: INTERNAL://:29092,EXTERNAL://:9092,CONTROLLER://:9093
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka:29092,EXTERNAL://localhost:9092
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"

  wiremock:
    image: wiremock/wiremock:3.10.0
    ports: ["8090:8080"]
    volumes:
      - ./wiremock/mappings:/home/wiremock/mappings:ro

  market:
    profiles: [app]   # docker-compose --profile app up
    build:
      context: ..
      dockerfile: infrastructure/Dockerfile
    depends_on:
      postgres: { condition: service_healthy }
      redis:    { condition: service_healthy }
      kafka:    { condition: service_started }
    environment:
      SPRING_PROFILES_ACTIVE: prod
      DB_HOST: postgres
      DB_NAME: market
      DB_USER: market
      DB_PASSWORD: market
      REDIS_HOST: redis
      KAFKA_BOOTSTRAP: kafka:29092   # 컨테이너끼리는 INTERNAL listener 로 접속
      PG_BASE_URL: http://wiremock:8080
      OAUTH_ISSUER_URI: http://localhost:9000   # 실제 IdP 로 교체
    ports: ["8080:8080"]

  # k6 부하 시나리오 — docker compose --profile load up k6.
  # 컨테이너 안에서 호스트의 BASE_URL 을 보려면 host.docker.internal 사용.
  # 본 service 는 단발성 실행 (default command 가 없어 무한 sleep 으로 살아 있다가
  # `docker compose run --rm k6 run /scripts/scenarios/<name>.js` 로 띄우는 게 보통).
  k6:
    profiles: [load]
    image: grafana/k6:0.50.0
    volumes:
      - ../load/k6:/scripts:ro
    environment:
      BASE_URL: ${BASE_URL:-http://host.docker.internal:8080}
      K6_TOKEN: ${K6_TOKEN:-}
      K6_SKUS: ${K6_SKUS:-}
      CONCURRENT_MATCH_SKU: ${CONCURRENT_MATCH_SKU:-00000000-0000-0000-0000-cafe00000099}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    # default 는 -h — 사용자가 `docker compose run --rm k6 run /scripts/scenarios/order-match.js`
    # 형태로 호출.
    entrypoint: ["k6"]
    command: ["-h"]
